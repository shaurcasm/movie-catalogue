# Getting started with Mock Movie Server

## Install Dependencies

Run `yarn install` or `npm install` in a terminal under the server directory.

## Start Server

Run `yarn start` or `npm start` in a terminal under the server directory.
